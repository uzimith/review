<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="UTF-8">
        <title>レビュー</title>
        <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
        <link rel="stylesheet" href="/assets/css/user.css">
    </head>
    <body>
        <header class="navbar navbar-default">
            <div class="container">
                <div class="navbar-header">
                    <a href="/" class="navbar-brand">Review</a>
                </div>
                <nav class="collapse navbar-collapse">
                    <ul class="nav navbar-nav navbar-right">
                        <% if current_user %>
                            <li>
                                <p class="navbar-text"><span class="glyphicon glyphicon-user"></span> <%= current_user.name %></p>
                            </li>
                            <li><a href="/sign_out"><span class="glyphicon glyphicon-log-out"></span> Sign out</a></li>
                        <% else %>
                            <li><a href="/sign_in"><span class="glyphicon glyphicon-log-in"></span> Sign in</a></li>
                        <li><a href="/sign_up"><span class="glyphicon glyphicon-new-window"></span> Sign up</a></li>
                        <% end %>
                    </ul>
                </nav>
            </div>
        </header>
        <div class="container">
            <%= yield %>
        </div>
    </body>
</html>
